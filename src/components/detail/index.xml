<view class="container">
    <view class="card">
        <!-- <view class="title-view">
            <view class="title">
                {{name}}
            </view>
            <view class="state-view">
                <l-tag bg-color="{{StateColorMp[iState]}}" shape="circle"> {{t('rsContract:v.iState.' + iState)}}</l-tag>
            </view>
        </view> -->
        <view class="card-content" wx:for="{{renderData}}">
            <view class="text-view" wx:if="{{item.type===image}}">
                <text class="label">{{item.label}}</text>
                <!-- <view class="img-view">
                    <block wx:for="{{tabImgs['drawing']}}" wx:key="index">
                        <image class="img" src="{{item.value}}" data-src="{{item.value}}" mode="aspectFit" bindtap="preview" />
                    </block>
                </view> -->
            </view>
            <view class="text-view">
                <text class="label">{{item.label}}</text>
                <text>{{item.value}}</text>
            </view>
           
        </view>
    </view>
</view>